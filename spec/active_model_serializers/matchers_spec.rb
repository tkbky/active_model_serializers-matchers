require "spec_helper"

describe ActiveModelSerializers::Matchers do
  it "has a version number" do
    expect(ActiveModelSerializers::Matchers::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
